# MapWelder

MapWelder is a simple Java tool to ensure that geographic maps described by sets of (multi)polygons with holes have properly shared boundaries.

# Dependencies

MapWelder is developed using GeometryCore (https://github.com/tue-alga/GeometryCore).
